version: "3.8"
services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/threats.db:/app/threats.db   # persist DB
      - ./backend/Cybersecurity_Dataset.csv:/app/Cybersecurity_Dataset.csv
